<!--components/index/tabbar.wxml-->
<view class="box">
  <view class="list">
    <view class='item {{index == page ? "active" : ""}}' data-index="{{index}}" wx:key="item" wx:for="{{list}}" catchtap="handleClick">{{item}}</view>
  </view>
</view>
